import React from 'react';

function ChatPanel({ docId }) {
  return (
    <div className="chat-panel">
      <h2>Interactive Chat</h2>
      <p>Ask details, clarifications, or summaries of the uploaded document.</p>
      {/* TODO: Add chat history feed and text inputs */}
    </div>
  );
}

export default ChatPanel;
